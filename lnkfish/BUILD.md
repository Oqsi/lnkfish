# 构建指引

本文档提供完整的构建和部署指引,帮助您在本地环境中编译和运行游戏。

## 🔧 环境要求

### 必需工具

1. **MoonBit 编译工具链**
   - 版本要求: >= 0.1.0
   - 官方网站: https://www.moonbitlang.com/
   - 安装指南: https://www.moonbitlang.com/docs/install

2. **Node.js**
   - 版本要求: >= 18.0.0
   - 用途: 运行本地开发服务器
   - 下载地址: https://nodejs.org/

3. **现代浏览器**
   - Chrome >= 90
   - Firefox >= 88
   - Safari >= 14
   - Edge >= 90
   - 需要支持 WebAssembly 和 Canvas 2D API

### 可选工具

- **Git**: 用于版本控制
- **VSCode**: 推荐的代码编辑器
- **MoonBit VSCode 插件**: 提供语法高亮和智能提示

## 📥 安装步骤

### 1. 安装 MoonBit

**Linux/macOS:**
```bash
# 使用官方安装脚本
curl -fsSL https://cli.moonbitlang.com/install/unix.sh | bash

# 添加到环境变量 (根据提示操作)
source ~/.bashrc  # 或 ~/.zshrc
```

**Windows:**
```powershell
# 使用 PowerShell 运行
irm https://cli.moonbitlang.com/install/windows.ps1 | iex
```

验证安装:
```bash
moon version
```

### 2. 安装 Node.js

访问 https://nodejs.org/ 下载并安装适合您系统的版本。

验证安装:
```bash
node --version
npm --version
```

### 3. 克隆项目

如果您有项目仓库地址:
```bash
git clone <repository-url>
cd lnkfish
```

或者如果您已经下载了项目压缩包:
```bash
# 解压后进入项目目录
cd lnkfish
```

## 🔨 构建游戏

### 基本构建

在项目根目录 (`lnkfish`) 执行:

```bash
# 构建 Release 版本 (JavaScript 目标)
moon build --target js

# 或使用简短命令
moon build -t js
```

构建成功后,编译产物将位于:
- `target/js/release/build/lnkfish.js` - 主要的 JavaScript 文件
- `target/js/release/build/lnkfish.d.ts` - TypeScript 类型定义文件

### 调试构建

如果需要调试版本 (包含更多调试信息):

```bash
moon build --target js --debug
```

### 清理构建产物

```bash
# 清理构建缓存
moon clean
```

### 完整重新构建

```bash
# 先清理再构建
moon clean && moon build --target js
```

## 🚀 运行游戏

### 方法一: 使用 http-server 

1. 安装 http-server (仅需一次):
```bash
npm install -g http-server
```

2. 在项目根目录启动服务器:
```bash
http-server -p 8080
```

3. 在浏览器中打开:
```
http://localhost:8080
```

### 方法二: 使用 Python 内置服务器

**Python 3:**
```bash
python -m http.server 8080
```

**Python 2:**
```bash
python -m SimpleHTTPServer 8080
```

然后访问 `http://localhost:8080`

### 方法三: 直接打开 (不推荐)

由于浏览器的安全限制,某些功能可能无法正常工作。如果仍要尝试:

```bash
# 在浏览器中打开
# Windows
start index.html

# macOS
open index.html

# Linux
xdg-open index.html
```

## 🛠️ 开发调试

### 文件监听和自动重新构建

使用 `moon` 的监听模式:

```bash
# 监听源文件变化并自动重新构建
moon build --target js --watch
```

在另一个终端启动服务器:
```bash
http-server -p 8080
```

### 浏览器开发者工具

1. 打开浏览器开发者工具 (F12)
2. 查看 Console 标签页了解日志输出
3. 使用 Network 标签页检查资源加载
4. 使用 Performance 标签页分析性能

### 常用调试技巧

1. **检查资源加载**
   - 确保所有图片和音频文件路径正确
   - 检查 Console 是否有 404 错误

2. **性能分析**
   - 监控帧率 (目标 60 FPS)
   - 检查内存使用情况

3. **移动端调试**
   - Chrome: chrome://inspect
   - Safari: 开发者菜单 > 连接设备

## 📦 部署发布

### 准备发布版本

1. **构建优化版本**:
```bash
moon build --target js --release
```

2. **需要部署的文件**:
```
lnkfish/
├── index.html
├── ThaleahFat.ttf
├── assets/
│   ├── *.png
│   ├── QIJIFALLBACK.ttf
│   └── (所有素材文件)
├── sounds/
│   └── *.wav, *.WAV
└── target/js/release/build/
    └── lnkfish.js
```

## 📚 进阶主题

### 修改游戏配置

编辑 `src/config.mbt` 文件可以调整:
- 视口尺寸
- 游戏难度参数
- 关卡设计
- 旋转速度等

修改后需要重新构建。

### 添加新关卡

1. 在 `src/config.mbt` 中定义新的障碍物和太极符序列
2. 在 `all_levels` 数组中添加新关卡
3. 重新构建并测试


**祝您构建顺利！**